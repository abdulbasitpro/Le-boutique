# Le Boutique Frontend Environment Variables Example
# Duplicate this file as '.env' or '.env.local' and adjust variables.

# The base URL for the backend API (used when fullstack node-express is connected)
VITE_API_URL=http://localhost:5000/api

# Enable mock dashboard statistics (true/false)
VITE_ENABLE_MOCK_DATA=true
